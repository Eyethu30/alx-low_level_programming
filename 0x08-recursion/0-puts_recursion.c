#include "main.h"

/**
* _puts_recursion -> this is a function to print string recursivly
* @s: string paramenter
*/
void _puts_recursion(char *s); { 
char i;

_putchar(s[i]);

for(i++, i != '\0',,)
{
_puts_recursion();
}
}
