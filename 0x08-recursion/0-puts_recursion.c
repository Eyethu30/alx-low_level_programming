#include <stdio.h>
#include "main.h"
#include <string.h>

/**
* _puts_recursion -> this is a function to print string recursivly
* @s: string paramenter
*/
void _puts_recursion(char *s)
{ 
    _putchar(char *s);
    _putchar('\n');
}
