#include <stdio.h>
#include "main.h"
#include <string.h>
#include "_putchar.c"

/**
* _puts_recursion -> this is a function to print string recursivly
* @s: string paramenter
*/
void new_char(char *s)
{
    write(1, &, 1);
}

void _puts_recursion(char *s)

{ 
if(char *s != '\0')
{
    
    new_char(*s);
    new_char('\n');

}
}
