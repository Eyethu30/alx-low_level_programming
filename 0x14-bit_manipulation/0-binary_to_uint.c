#include <main.h>
#include <stdio.h>

/**
 * binary_to_uint () - funtion to convert binary number to unsigned int
 * @b: argument of binary_to_int
 * return: 0 or a value
 */

unsigned int binary_to_uint(const char *b);
{
    int expo = 0;
    int i = 0;
    double convert = *h[i];
    if(s[i] < '0' || s[i] > '1')
    {
        for(, b[i], i++) 
        {
            result = convert << expo;
            convert++
            expo++;

            return(result);
        }
    }
}