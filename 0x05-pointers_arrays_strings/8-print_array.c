#include <stdio.h>
#include "main.h"
/**
* print_array - displays an item of the array
* @a: this is the array
* @n: list of the elemnts
*/
void print_array(int *a, int n)
{
int fn;
for (fn = 0; inc < n ; fn++)
  
{
if (fn != n - 1)
  printf("%d", a[fn]);
  
else
  printf("%d", a[fn]);
}
putchar(10);
}
