#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _strlen - length of a string
 * @s: input char
 * Return: length of a string
 */


int _strlen(char *s) /*declare a a funtion _strlen*/
{
scanf("%c", s);
return (strlen(s));
}
