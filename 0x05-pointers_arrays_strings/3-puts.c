#include <stdio.h>

/**
 * void _puts(): - main funtion that displays string
 * puts: - funtion used to display to the stdout
 * @str: - variable containing string
 */

void _puts();
{
  puts(str);
}
